<% layout('layouts/boilerplate') %>

    <div class="row">
        <div class="col-md-12">
            <div class="card-shadow">
                <div class="form-group clearfix">
                    <a href="/ingridient/new" class="btn btn-primary float-right">Add <%= title %></a>
                </div>
                <div class="card-body">
                    <h5 class="card-title">List <%= title %></h5>
                    <div class="table-responsive">
                        <table id="table" class="display table" width="100%">
                            <thead>
                                <tr class="text-center">
                                    <th style="width: 10%;">No</th>
                                    <th style="width: 40%;">Name</th>
                                    <th style="width: 30%;">Price/Qty</th>
                                    <th style="width: 20%;">Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% for(let i = 0; i < ingridients.length; i++ ) { %>
                                    <tr>
                                        <td class="text-center"><%= i+1 %></td>
                                        <td><%= ingridients[i].name%></td>
                                        <td><%= ingridients[i].price%></td>
                                        <td>
                                            <div class="d-flex justify-content-center align-items-center">
                                                <a href="/ingridient/<%= ingridients[i]._id %>"><i class="fa fa-eye mx-1"></i></a>
                                                <a href="/ingridient/<%= ingridients[i]._id %>/edit"><i class="fa fa-edit text-warning mx-1"></i></a>
                                                <form action="/ingridient/<%= ingridients[i]._id %>?_method=DELETE" class="d-inline mx-1" method="POST">
                                                    <button style="border: none;"><i class="fa fa-trash text-danger"></i></button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                <% } %> 
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://js.pusher.com/4.1/pusher.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.16.2/axios.js"></script>
    <script src="/js/ingridient/index.js"></script>